# Copy to secrets.yaml for local simulate runs. NEVER commit secrets.yaml.
# In production these live encrypted in the workflow's secrets store — the kill
# levels are the desk's private policy and only ever exist inside the TEE.
MAP_SIDE: "long"
KILL: "2500"
NET: "4"
