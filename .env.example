# The Graph — Studio / Market. Never commit the real key.
GRAPH_GATEWAY_URL=https://gateway.thegraph.com/api
GRAPH_API_KEY=
# Standardized token/balance subgraph (Messari or official shared schema). Prize compose #2.
GRAPH_STANDARD_SUBGRAPH_ID=
# Our maps + conflict subgraph (this repo, Studio).
GRAPH_MAPS_SUBGRAPH_ID=
