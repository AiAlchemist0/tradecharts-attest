# Copy to .env — gitignored, never commit.

# --- CRE simulate (cre/README.md) ---
# Throwaway key for the local simulator only; never a real wallet.
# CRE_ETH_PRIVATE_KEY=
# Close-kill policy — the workflow's TEE secrets (via cre/secrets.yaml).
# MAP_SIDE=long
# KILL=2500
# NET=4
# Mock close server.
# MOCK_PORT=8787
# MOCK_CLOSE=2400
# MOCK_SYMBOL=ETH

# --- Optional Graph Network (Aave / source=aave|both) ---
# Not required for demo/, npm run compose, or MCP source=base (those are keyless Studio).
# GRAPH_API_KEY=
